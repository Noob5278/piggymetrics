# Example environment file. Copy to `.env` and override the placeholder
# values with strong, unique secrets for your environment. The `.env` file
# is gitignored and must NOT be committed.
CONFIG_SERVICE_PASSWORD=changeme
NOTIFICATION_SERVICE_PASSWORD=changeme
STATISTICS_SERVICE_PASSWORD=changeme
ACCOUNT_SERVICE_PASSWORD=changeme
MONGODB_PASSWORD=changeme
